<Window x:Class="MasterDocumentation.Views.TextPrompt"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
 Title="Введите значение" Width="560" Height="210" MinWidth="420" MinHeight="190" Style="{x:Null}"
 WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="CanResize"
 Background="{DynamicResource Background/App}" Foreground="{DynamicResource Text/Primary}">
 <shell:WindowChrome.WindowChrome>
  <shell:WindowChrome CaptionHeight="46" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0"/>
 </shell:WindowChrome.WindowChrome>
 <Border BorderBrush="{DynamicResource Border/Default}" BorderThickness="1" Background="{DynamicResource Background/App}">
  <Grid>
   <Grid.RowDefinitions><RowDefinition Height="46"/><RowDefinition Height="*"/><RowDefinition Height="62"/></Grid.RowDefinitions>
   <Border BorderBrush="{DynamicResource Border/Default}" BorderThickness="0,0,0,1" Background="{DynamicResource Background/SurfaceElevated}">
    <Grid Margin="16,0,6,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
     <TextBlock Text="{Binding Title,RelativeSource={RelativeSource AncestorType=Window}}" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
     <Button Grid.Column="1" Content="×" Width="34" Height="34" MinHeight="34" Padding="0" Margin="2" FontSize="19" IsCancel="True" shell:WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Закрыть"/>
    </Grid>
   </Border>
   <Grid Grid.Row="1" Margin="18,16,18,8">
    <TextBox x:Name="Input" MinHeight="48" Padding="13,10" VerticalContentAlignment="Center" TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"/>
   </Grid>
   <Border Grid.Row="2" BorderBrush="{DynamicResource Border/Default}" BorderThickness="0,1,0,0" Background="{DynamicResource Background/Surface}">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="14,8">
     <Button Content="Отмена" IsCancel="True" MinWidth="96"/>
     <Button Content="OK" IsDefault="True" Click="Ok_Click" MinWidth="96" Style="{StaticResource PrimaryButtonStyle}"/>
    </StackPanel>
   </Border>
  </Grid>
 </Border>
</Window>
